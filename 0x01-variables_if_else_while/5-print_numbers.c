#include <stdio.h>
/**
 * main - program that prints all single digits nums starting from 0 of base 10
 * Return: 0 (Success)
 */
int main (void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf('\n');
	return (0);
}
