#include "main.h"
/**
 * 6-abs.c - Function that computes the absolute value of an integer
 * @ab: integer input
 * Return: Always 0
 */
int _abs(int)
{
	if (c < 0)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
	return (c);
}
